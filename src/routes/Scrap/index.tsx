import FilterList from '../../components/FilterList';
import { ScrapContainer } from './style';

const Scrap = () => {
	return (
		<ScrapContainer>
			<FilterList />
		</ScrapContainer>
	);
};

export default Scrap;
